# Виключаємо session.slice (gnome-shell, dbus, gsd-*) з кандидатів systemd-oomd.
# omit = ніколи не вбивати; саме через ці юніти падіння одного процесу
# перетворювалось на розлогінення.
[Slice]
ManagedOOMPreference=omit
